{
  "config": {
    "step": {
      "user": {
        "title": "Fritz!Box Mesh Setup",
        "description": "Enter your Fritz!Box connection details.",
        "data": {
          "host": "Host",
          "port": "Port",
          "username": "Username",
          "password": "Password",
          "use_tls": "Use TLS (HTTPS)",
          "poll_interval": "Poll interval (seconds)",
          "debug_mode": "Debug raw JSON output",
          "debug_use_json": "Use debug JSON as topology source",
          "debug_json_path": "Debug JSON file path"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Fritz!Box. Check host, port and TLS setting.",
      "invalid_auth": "Invalid username or password.",
      "invalid_debug_json": "Debug JSON is invalid or file path is not readable.",
      "unknown": "Unexpected error — check the Home Assistant logs."
    },
    "abort": {
      "already_configured": "This Fritz!Box is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Fritz!Box Mesh Options",
        "description": "Adjust polling and debug output.",
        "data": {
          "poll_interval": "Poll interval (seconds)",
          "debug_mode": "Debug raw JSON output",
          "debug_use_json": "Use debug JSON as topology source",
          "debug_json_path": "Debug JSON file path"
        }
      }
    }
  }
}
